version: '3.7'

services:
  mongo:
    image: mongo:latest
    restart: always
    ports:
      - 27017:27017
    volumes:
      - ./db/:/docker-entrypoint-initdb.d/:ro
      - mongodb_data:/data/db

  api:
    build:
      context: ./api
      dockerfile: dockerfile
    ports:
      - 3001:3001
    volumes:
      - ./api/src:/app/src
    depends_on:
      - mongo


volumes:
  mongodb_data: